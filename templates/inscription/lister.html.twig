<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES INSCRIPTIONS</br>
		<table>
		{% for i in pInscription %}
		<tr>
			<td><a href="{{ path('inscriptionConsulter', { 'id': i.id }) }}">{{ i.nombreDePaiements }}</a></td>
	{% else %}
		<tr>
		<td>Aucune inscription n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	 
    </body>
</html>