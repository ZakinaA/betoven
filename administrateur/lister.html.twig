<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES PROFESSEURS</br>
		<table>
		{% for e in pProfesseurs %}
		<tr>
			<td><a href="{{ path('professeurConsulter', { 'id': e.id }) }}">{{ e.nom }}</a></td>
			<td>{{ e.prenom }}</a></td>
			<td>{{ e.dateNaiss|date('d/m/Y') }}</td>
	{% else %}
		<tr>
		<td>Aucun professeur n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	 
    </body>
</html>